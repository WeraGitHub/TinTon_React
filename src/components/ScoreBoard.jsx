import React from "react";
import "./ScoreBoard.css";

const ScoreBoard = ({ scoreBoard }) => (
  <div className="game-over-modal-mask">
    <p>hello</p>
    <button onClick={scoreBoard}>Close</button>
  </div>
);

export default ScoreBoard;
